Backbone.sync = function (action, model, success, error) {
  console.log(action, typeof model);
  // load sample data.
  
  // clear all data.
  
  // retrieve all tasks.
  
  // create new task.
  if (action == 'create' && typeof mode == Task) {
    console.log('create task');
  }
  
  // update task (set completed).
}